#include <stdio.h>
#include <iostream>
#include <stdlib.h>
#include <vector>
#include <algorithm>
#include <stack>
#include <queue>
#include <unordered_map>
#include <unordered_set>
#include <map>
#include <set>
#include <string.h>
using namespace std;



#define N 100000


int cnt[3][N];
class Solution1 {
public:
    /**
     * 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可
     * 
     * @param x string字符串 
     * @return int整型
     */
    int Maximumlength(string x) {
        // write code here
        memset(cnt[0],0,N*sizeof(int));
        memset(cnt[0],0,N*sizeof(int));
        memset(cnt[0],0,N*sizeof(int));
        int low=0,high=0,ans=0;
    }
};
















int main(){

}